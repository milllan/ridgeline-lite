---
/**
 * Home page — Lite edition (6 sections): Hero → TrustBar → Services →
 * WhyUs → Reviews → FinalCTA. The Pro version adds featured
 * before/after projects, the service-area system, and financing.
 */
import { getCollection } from 'astro:content';
import { getImage } from 'astro:assets';
import heroImage from '../assets/hero/home-hero.jpg';
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/sections/Hero.astro';
import TrustBar from '../components/sections/TrustBar.astro';
import ServicesGrid from '../components/sections/ServicesGrid.astro';
import WhyUs from '../components/sections/WhyUs.astro';
import ReviewsCarousel from '../components/reviews/ReviewsCarousel.astro';
import FinalCTA from '../components/sections/FinalCTA.astro';
import SectionHeading from '../components/common/SectionHeading.astro';
import { siteConfig } from '../config/site';

const services = (await getCollection('services')).sort(
  (a, b) => a.data.order - b.data.order,
);

// Preload the hero image (the LCP element).
const heroPreload = await getImage({
  src: heroImage,
  widths: [768, 1080, 1440, 1920],
  sizes: '100vw',
  quality: 55,
});
---

<BaseLayout
  title={siteConfig.seo.defaultTitle}
  description={siteConfig.seo.defaultDescription}
>
  <link
    slot="head"
    rel="preload"
    as="image"
    imagesrcset={heroPreload.srcSet.attribute}
    imagesizes="100vw"
    fetchpriority="high"
  />
  <Hero />
  <TrustBar />

  <section class="bg-surface py-16 md:py-20">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <SectionHeading
        eyebrow="Šta radimo"
        title="Popravka veš mašina svih marki"
        lead="Od curenja i buke do mašine koja ne pokreće bubanj — dolazimo na adresu, utvrđujemo kvar i jasno vam kažemo šta treba popraviti."
        align="center"
      />
      <div class="mt-12">
        <ServicesGrid services={services} />
      </div>
    </div>
  </section>

  <WhyUs />
  <ReviewsCarousel />
  <FinalCTA />
</BaseLayout>
